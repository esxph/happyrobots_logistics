# Remote Ubuntu deploy (optional — do not commit)
# Copy to deploy.env and fill in. Used by scripts/remote-deploy.sh

DEPLOY_SSH_HOST=your.server.ip.or.hostname
DEPLOY_SSH_USER=ubuntu
DEPLOY_SSH_PORT=22
DEPLOY_SSH_KEY_PATH=~/.ssh/id_rsa
DEPLOY_REMOTE_DIR=/opt/happyrobots-carrier-sales
